# Tests

Place all automated tests here.

Recommended layout:

- `tests/unit/` for pure logic and schema validation tests
- `tests/integration/` for graph/tool/database integration tests

Suggested first tests:

- quote-to-book consistency checks
- HITL interrupt/resume flow checks
- shipment details verification checks
